#include <iostream>
#include <cmath>

#ifndef PRINT
  #define PRINT(var) cout << #var ": " << var << endl
#endif

using namespace std;

// Checking the double representation
int main (void) {
  cout << hexfloat;
  PRINT(ldexp(1.0, 26));
  PRINT(67108864.0);
  PRINT(0.0000001);
  PRINT(0.1111111);
  PRINT(ldexp(1.0, 26) * 67108864.0);
  PRINT(ldexp(1.0, 26) * 0.0000001);
  PRINT(static_cast<size_t>(ldexp(1.0, 26) * 0.0000001));
  PRINT(ldexp(1.0, 26) * 0.1111111);
  PRINT(static_cast<size_t>(ldexp(1.0, 26) * 0.1111111));
  // 0X1.2BD7E1CB5AB1P-4 - smallest item profit between all the pricings
  // generated by executing the column generation over instance
  // BPP_1000_1000_0.1_0.7_0.csp (eqv: 0.0732039)
  PRINT(static_cast<size_t>(ldexp(1.0, 26) * 0.0732039));
  // The highest item profit value found was 1.0, so 2^26 is the highest
  // value when converted to integer, and as rarely the objective function
  // reachs 2, this seem to be a very safe margin.
}

